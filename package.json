{
  "name": "my-lambda",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install-all": "npm install && bower --allow-root install && cd front && tsd install && cd ../back && tsd install",
    "front-watch-typescript": "tsc -p front --watch ",
    "front-typescript": "tsc -p front",
    "front-watch-browserify": "watchify tmp/front/js/application.js -d -o front/public/app.js",
    "front-browserify-dev": "browserify -d -e tmp/front/js/application.js -o front/public/app.js",
    "front-browserify-prod": "browserify -e tmp/front/js/application.js -o front/public/app.js",
    "front-build-dev": "npm run front-clean && npm run front-typescript && npm run front-browserify-dev",
    "front-build-prod": "npm run front-clean && npm run front-typescript && npm run front-browserify-prod && npm run front-uglify",
    "front-clean": "rimraf front/public/app.js && rimraf tmp/front",
    "front-serve": "http-server front/public",
    "front-watch": "npm run front-build-dev && parallelshell \"npm run front-watch-typescript\" \"npm run front-watch-browserify\" \"npm run front-serve\" ",
    "front-uglify": "uglify -s front/public/app.js -o front/public/app.js",
    "back-typescript": "tsc -p back",
    "back-typescript-watch": "tsc -p back --watch",
    "back-build-dev": "npm run back-typescript",
    "back-clean": "rimraf back/lib",
    "back-serve": "nodemon -w back/lib index.js -d 0.1",
    "watch": "npm run back-build-dev && npm run front-build-dev && parallelshell \"npm run back-typescript-watch\"  \"npm run back-serve\" \"npm run front-watch-typescript\" \"npm run front-watch-browserify\"",
    "build": "npm run front-build-prod && npm run back-build-dev",
    "update-library": "node ./scripts/update-library.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "bower": "^1.6.3",
    "browserify": "^11.2.0",
    "http-server": "^0.8.5",
    "nodemon": "^1.8.0",
    "parallelshell": "^2.0.0",
    "rimraf": "^2.4.3",
    "tsd": "^0.6.5",
    "typescript": "^1.6.2",
    "uglify": "^0.1.5",
    "watchify": "^3.4.0"
  },
  "dependencies": {
    "body-parser": "^1.14.1",
    "express": "^4.13.3",
    "glob": "^5.0.15",
    "lodash": "^3.10.1",
    "mkdirp": "^0.5.1",
    "musicmetadata": "^2.0.2",
    "npm": "^3.3.9",
    "react": "^0.13.3",
    "react-router": "^0.13.3",
    "uuid": "^2.0.1"
  }
}
